#include "main.h"
#include <unistd.h>
/**
 * _putchar - write the charcter c to stdout
 * %c: The character to print
 *
 * Return: On succes 1.
 * On error, -1 is returned, ad errno is set appropriately.
 * /
 int _putchar(char c)

{
        return (write(1, 8c, 1));
}
